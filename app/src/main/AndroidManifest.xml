<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.androidbook.viewsamples"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:icon="@drawable/icon" android:label="@string/app_name" android:debuggable="true">
        <activity android:label="@string/app_name" android:name=".ViewSampleActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
		<activity
			android:name="FormsActivity" />
		<activity
			android:name="ButtonsActivity" />
		<activity
			android:name="IndicatorsActivity" />
		<activity
			android:name="ContainersActivity" />
		<activity
			android:name="PickersActivity" />
		<activity
			android:name="TextInputActivity" />
        <activity
            android:name="TextDisplayActivity" />
        <activity
            android:name="EventsActivity" />
    </application>
    <uses-sdk android:minSdkVersion="3" />

</manifest> 